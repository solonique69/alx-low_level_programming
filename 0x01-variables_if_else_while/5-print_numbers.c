#include <stdio.h>

/**
 * main - Entry point
 * Description; A C-program that counts single digits
 * Return: Always 0 (Success)
 */

int main(void)
{
	int FirsTen;

	for (FirsTen = 0; FirsTen < 10; FirsTen++)
	{
		printf("%d", FirsTen);
	}

	printf("\n");

	return (0);
}
